diff --git a/docs/sprints/S06/README.md b/docs/sprints/S06/README.md
index d204a39..45a2fed 100644
--- a/docs/sprints/S06/README.md
+++ b/docs/sprints/S06/README.md
@@ -4,7 +4,7 @@
 Implement full git/worktree preflight automation inside the engine using the S05 receipts executor: primary worktree command-set validation with hard-stop flows, sprint branch-prefix compliance with remediation guidance, prune/worktree sanity policy checks, and deterministic `OUT_OF_SYNC` event emission.
 
 ## Work Plan
-- **S06.A Primary worktree automation**: Execute the canonical command set against `~/Projects/promptops-gui` and emit structured pass/hard-stop outcomes with receipts.
+- **S06.A Primary worktree automation**: Execute the canonical command set against `PROMPTOPS_REPO` (resolved git toplevel), emit structured pass/hard-stop outcomes with receipts, and avoid hardcoded absolute path assumptions.
 - **S06.B Branch compliance gate**: Validate both `^sprint/S\\d{2}-` and `^sprint/S06-`; emit `HARD_STOP_BRANCH_NONCOMPLIANCE` plus remediation guidance if invalid.
 - **S06.C Prune/worktree sanity policy**: Always record `git worktree list --porcelain`; run `git prune --dry-run` when policy is `ON`, otherwise emit `PRUNE_DISABLED`.
 - **S06.D Out-of-sync detection**: Capture baseline fingerprints and emit deterministic `OUT_OF_SYNC` on unexpected mid-run repository changes.
diff --git a/docs/sprints/S07/README.md b/docs/sprints/S07/README.md
index 5cd9757..3eb040b 100644
--- a/docs/sprints/S07/README.md
+++ b/docs/sprints/S07/README.md
@@ -8,7 +8,7 @@ Implement Open PR Handling Protocol in-engine with GUI-visible status, determini
 - **S07.B Readiness**: Evaluate merge readiness from `gh pr view` + thread GraphQL receipts with explicit failing conditions.
 - **S07.C Merge gate**: Expose merge attempt capability guarded by `operatorApprovedMerge=true` (default false).
 - **S07.D Codex protocol**: Detect codex comments/threads, identify unresolved codex threads, resolve via GraphQL where possible, and stop with deterministic blocker when unresolved.
-- **S07.E Canonical root policy**: Resolve canonical root via `PROMPTOPS_GUI_CANONICAL_ROOT` or git toplevel fallback and enforce cwd guardrails.
+- **S07.E Canonical root policy**: Resolve canonical root from `PROMPTOPS_REPO` (mandatory) and validate against git toplevel before running protocol steps.
 
 ## Acceptance Tests
 - **AT-S07-01**: Unrelated open PR inventory triggers deterministic stop with evidence.
diff --git a/docs/sprints/S08/README.md b/docs/sprints/S08/README.md
index d4ac039..1661e25 100644
--- a/docs/sprints/S08/README.md
+++ b/docs/sprints/S08/README.md
@@ -8,6 +8,7 @@ Add deterministic programmatic invocation for Codex (primary) and Claude (second
 - `docs/sprints/S08/**` for durable sprint evidence.
 - No new endpoints/routes.
 - No new dependencies.
+- Root remediation delta: operator scripts must resolve repository root from `PROMPTOPS_REPO` + git toplevel (no hardcoded `~/Projects` assumptions).
 
 ## Canonical Contract (S08.A)
 - Input envelope fields: rendered ticket JSON, continuity packet/hash, repo context snapshot, policy bundle, evidence ledger snapshot, run timeline tail, checkpoint id, outstanding deltas, no-rework directive.
@@ -58,3 +59,4 @@ Hashing rules:
 - Staging: `/tmp/promptops/S08/**`
 - Durable: `docs/sprints/S08/evidence/**`
 - PR readiness + codex thread resolution: `docs/sprints/S08/evidence/pr_readiness/**`
+- Migration guidance: `docs/sprints/S08/MIGRATION_PLAYBOOK.md`
diff --git a/scripts/migration-sim.sh b/scripts/migration-sim.sh
index db0fc47..072ec9e 100644
--- a/scripts/migration-sim.sh
+++ b/scripts/migration-sim.sh
@@ -1,8 +1,17 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
-SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
+if [[ -z "${PROMPTOPS_REPO:-}" ]]; then
+  echo "HARD STOP: MISSING_REPO_ROOT (set PROMPTOPS_REPO before running migration simulation)." >&2
+  exit 41
+fi
+
+REPO_ROOT="$(cd "$PROMPTOPS_REPO" && pwd -P)"
+if ! TOPLEVEL_RAW="$(git -C "$REPO_ROOT" rev-parse --show-toplevel 2>/dev/null)"; then
+  echo "HARD STOP: NOT_A_GIT_REPO ('$REPO_ROOT')." >&2
+  exit 43
+fi
+REPO_ROOT="$(cd "$TOPLEVEL_RAW" && pwd -P)"
 
 bash "$REPO_ROOT/scripts/preflight.sh"
 
diff --git a/scripts/preflight.sh b/scripts/preflight.sh
index e1a9c6b..29fcd22 100644
--- a/scripts/preflight.sh
+++ b/scripts/preflight.sh
@@ -1,10 +1,8 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
-SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
-STATE_DIR="$REPO_ROOT/.agent_state"
-PROBE_FILE="$STATE_DIR/preflight.write_probe"
+BASE_BRANCH="${PROMPTOPS_BASE_BRANCH:-main}"
+CORRUPTION_PATTERN='bad sha1|invalid ref|badRefName|badRefContent'
 
 need_cmd() {
   local cmd="$1"
@@ -19,20 +17,102 @@ need_cmd node
 need_cmd npm
 need_cmd rg
 
-if ! git -C "$REPO_ROOT" rev-parse --show-toplevel >/dev/null 2>&1; then
-  echo "preflight failure: '$REPO_ROOT' is not a git repository root" >&2
-  exit 11
+if [[ -z "${PROMPTOPS_REPO:-}" ]]; then
+  echo "HARD STOP: MISSING_REPO_ROOT (set PROMPTOPS_REPO to the intended repository root)." >&2
+  exit 41
 fi
 
-TOPLEVEL="$(git -C "$REPO_ROOT" rev-parse --show-toplevel)"
+if [[ ! -d "$PROMPTOPS_REPO" ]]; then
+  echo "HARD STOP: MISSING_REPO_ROOT (path '$PROMPTOPS_REPO' does not exist)." >&2
+  exit 42
+fi
+
+REPO_ROOT="$(cd "$PROMPTOPS_REPO" && pwd -P)"
+
+run_probe() {
+  local label="$1"
+  shift
+  echo "### $label"
+  echo "CMD: $*"
+  set +e
+  "$@"
+  local ec=$?
+  set -e
+  echo "EXIT_CODE:$ec"
+  echo
+  return "$ec"
+}
+
+if ! TOPLEVEL_RAW="$(git -C "$REPO_ROOT" rev-parse --show-toplevel 2>/dev/null)"; then
+  echo "HARD STOP: NOT_A_GIT_REPO ('$REPO_ROOT')." >&2
+  exit 43
+fi
+TOPLEVEL="$(cd "$TOPLEVEL_RAW" && pwd -P)"
 if [[ "$TOPLEVEL" != "$REPO_ROOT" ]]; then
-  echo "preflight failure: script-derived root '$REPO_ROOT' != git root '$TOPLEVEL'" >&2
-  exit 12
+  echo "HARD STOP: NOT_A_GIT_REPO_ROOT (PROMPTOPS_REPO='$REPO_ROOT' git_toplevel='$TOPLEVEL')." >&2
+  exit 44
 fi
 
-mkdir -p "$STATE_DIR"
-echo "probe" >"$PROBE_FILE"
-rm -f "$PROBE_FILE"
+run_probe "pwd" pwd
+echo "### env_repo_root"
+echo 'CMD: echo $PROMPTOPS_REPO'
+echo "${PROMPTOPS_REPO}"
+echo "EXIT_CODE:0"
+echo
+run_probe "show_toplevel" git -C "$REPO_ROOT" rev-parse --show-toplevel
+
+status_out="$(git -C "$REPO_ROOT" status --porcelain=v1 --branch)"
+echo "### status_branch"
+echo "CMD: git -C \"$REPO_ROOT\" status --porcelain=v1 --branch"
+printf '%s\n' "$status_out"
+echo "EXIT_CODE:0"
+echo
+
+run_probe "fetch_all" git -C "$REPO_ROOT" fetch --all --prune --tags
+
+ahead_behind_out="$(git -C "$REPO_ROOT" rev-list --left-right --count "HEAD...origin/${BASE_BRANCH}")"
+echo "### ahead_behind_vs_main"
+echo "CMD: git -C \"$REPO_ROOT\" rev-list --left-right --count HEAD...origin/${BASE_BRANCH}"
+printf '%s\n' "$ahead_behind_out"
+echo "EXIT_CODE:0"
+echo
+
+run_probe "worktree_list" git -C "$REPO_ROOT" worktree list --porcelain
+
+set +e
+prune_out="$(git -C "$REPO_ROOT" prune --dry-run 2>&1)"
+prune_ec=$?
+set -e
+echo "### prune_dry_run"
+echo "CMD: git -C \"$REPO_ROOT\" prune --dry-run"
+printf '%s\n' "$prune_out"
+echo "EXIT_CODE:$prune_ec"
+echo
+
+set +e
+fsck_out="$(git -C "$REPO_ROOT" fsck --no-reflogs 2>&1)"
+fsck_ec=$?
+set -e
+echo "### fsck_no_reflogs"
+echo "CMD: git -C \"$REPO_ROOT\" fsck --no-reflogs"
+printf '%s\n' "$fsck_out"
+echo "EXIT_CODE:$fsck_ec"
+echo
+
+branch="$(git -C "$REPO_ROOT" rev-parse --abbrev-ref HEAD)"
+dirty_count="$(printf '%s\n' "$status_out" | tail -n +2 | sed '/^$/d' | wc -l | tr -d ' ')"
+ahead="$(printf '%s\n' "$ahead_behind_out" | awk '{print $1}')"
+behind="$(printf '%s\n' "$ahead_behind_out" | awk '{print $2}')"
+
+if [[ "$branch" != "$BASE_BRANCH" || "$ahead" != "0" || "$behind" != "0" || "$dirty_count" != "0" ]]; then
+  echo "HARD STOP: PRIMARY WORKTREE NOT ON MAIN/NOT SYNCED (branch=$branch ahead=$ahead behind=$behind dirty=$dirty_count)." >&2
+  exit 45
+fi
+
+if [[ "$prune_ec" -ne 0 || "$fsck_ec" -ne 0 ]] || printf '%s\n%s\n' "$prune_out" "$fsck_out" | rg -qi "$CORRUPTION_PATTERN"; then
+  echo "HARD STOP: GIT OBJECT HEALTH (corruption signals detected in prune/fsck output)." >&2
+  exit 46
+fi
 
 if [[ "$REPO_ROOT" == /Volumes/* ]]; then
   MOUNT_ROOT="$(echo "$REPO_ROOT" | awk -F/ '{print "/"$2"/"$3}')"
diff --git a/scripts/s07-open-pr-protocol.ts b/scripts/s07-open-pr-protocol.ts
index d7454b2..e915f03 100644
--- a/scripts/s07-open-pr-protocol.ts
+++ b/scripts/s07-open-pr-protocol.ts
@@ -27,7 +27,7 @@ function parseArgs(argv: string[]): CliOptions {
     stagingRoot,
     durableRoot: get('--durable-root'),
     baseBranch: get('--base-branch') ?? 'main',
-    canonicalRootEnv: get('--canonical-root-env') ?? process.env.PROMPTOPS_GUI_CANONICAL_ROOT,
+    canonicalRootEnv: get('--canonical-root-env') ?? process.env.PROMPTOPS_REPO ?? process.env.PROMPTOPS_GUI_CANONICAL_ROOT,
     operatorApprovedMerge: has('--operator-approved-merge'),
     enforceMainPreflight: !has('--skip-main-preflight'),
     resolveCodexThreads: !has('--skip-codex-resolve'),
diff --git a/scripts/verify.sh b/scripts/verify.sh
index a20055b..cf48a11 100644
--- a/scripts/verify.sh
+++ b/scripts/verify.sh
@@ -1,7 +1,22 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
-ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+if [[ -z "${PROMPTOPS_REPO:-}" ]]; then
+  echo "HARD STOP: MISSING_REPO_ROOT (set PROMPTOPS_REPO to the intended repository root)." >&2
+  exit 41
+fi
+
+if [[ ! -d "$PROMPTOPS_REPO" ]]; then
+  echo "HARD STOP: MISSING_REPO_ROOT (path '$PROMPTOPS_REPO' does not exist)." >&2
+  exit 42
+fi
+
+ROOT_DIR="$(cd "$PROMPTOPS_REPO" && pwd -P)"
+if ! TOPLEVEL_RAW="$(git -C "$ROOT_DIR" rev-parse --show-toplevel 2>/dev/null)"; then
+  echo "HARD STOP: NOT_A_GIT_REPO ('$ROOT_DIR')." >&2
+  exit 43
+fi
+ROOT_DIR="$(cd "$TOPLEVEL_RAW" && pwd -P)"
 cd "$ROOT_DIR"
 
 MODE="${1:-all}"
diff --git a/src/s07/open_pr_protocol.ts b/src/s07/open_pr_protocol.ts
index 6c0dd13..d779312 100644
--- a/src/s07/open_pr_protocol.ts
+++ b/src/s07/open_pr_protocol.ts
@@ -189,7 +189,8 @@ function resolveConfig(config: ProtocolConfig): Required<ProtocolConfig> {
   const cwd = path.resolve(config.cwd);
   const stagingRoot = path.resolve(config.stagingRoot);
   const durableRoot = path.resolve(config.durableRoot ?? path.join(cwd, 'docs', 'sprints', 'S07', 'evidence'));
-  const canonicalRootEnv = config.canonicalRootEnv ?? process.env.PROMPTOPS_GUI_CANONICAL_ROOT ?? '';
+  const canonicalRootEnv =
+    config.canonicalRootEnv ?? process.env.PROMPTOPS_REPO ?? process.env.PROMPTOPS_GUI_CANONICAL_ROOT ?? '';
   const genericSprintRegex = config.genericSprintRegex ?? '^sprint/S\\d{2}-';
   const sprintRegex = config.sprintRegex ?? '^sprint/S07-';
   return {
