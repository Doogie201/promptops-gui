# S16 Evidence Index

- `preflight/` — repo root, sync, worktree, prune/fsck, and open PR inventory receipts.
- `at/` — AT-S16-01..03 command and JSON proof receipts.
- `gates/` — `pre-commit`, `npm run lint`, `npm test`, `npm run build`.
- `scope/` — changed-file whitelist and budget checks.
- `maintainability/` — touched-file LOC and function-length budget checks.
- `stoplight/` — marker list and required grep outputs.
- `regression/` — deterministic replay fixture hashes.
- `hardening/` — long-run stability and recovery-action evidence.
- `pr/` — PR creation, labels, checks, thread status, and stability watch.
