CMD: gh api graphql -f query='query($owner:String!, $repo:String!, $number:Int!){ repository(owner:$owner,name:$repo){ pullRequest(number:$number){ reviewThreads(first:100){ nodes { id isResolved comments(first:20){ nodes { author { login } body url } } } } } } }' -F owner=Doogie201 -F repo=promptops-gui -F number=19
CWD: /Volumes/X9_DEV/Dev/Projects/promptops-gui
{"data":{"repository":{"pullRequest":{"reviewThreads":{"nodes":[{"id":"PRRT_kwDORZ8p_M5yOLLa","isResolved":true,"comments":{"nodes":[{"author":{"login":"chatgpt-codex-connector"},"body":"**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Canonicalize allowed actions before hashing diagnosis report**\n\nThe safe-mode report is documented as deterministic, but `next_allowed_actions` is serialized directly from `input.allowedActions` without canonical ordering, so the same logical action set produces different `reportHash` values when callers provide a different order (for example, actions aggregated from a map/set or UI reorder). This breaks hash stability for equivalent diagnoses and can cause false drift/continuity mismatches.\n\nUseful? React with 👍 / 👎.","url":"https://github.com/Doogie201/promptops-gui/pull/19#discussion_r2887425814"}]}},{"id":"PRRT_kwDORZ8p_M5yOLLb","isResolved":true,"comments":{"nodes":[{"author":{"login":"chatgpt-codex-connector"},"body":"**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Return cloned repair actions to avoid shared-state mutation**\n\n`actionsForSignal` returns references from `REPAIR_ACTION_LIBRARY` instead of copies, so any caller-side mutation (for example, appending to `commandPlan` for display or instrumentation) mutates global shared objects and changes subsequent calls. This introduces cross-request state leakage and non-deterministic remediation plans over time.\n\nUseful? React with 👍 / 👎.","url":"https://github.com/Doogie201/promptops-gui/pull/19#discussion_r2887425815"}]}},{"id":"PRRT_kwDORZ8p_M5yOLLd","isResolved":true,"comments":{"nodes":[{"author":{"login":"chatgpt-codex-connector"},"body":"**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Validate loop threshold before slicing detection window**\n\nWhen `threshold` is `0` (or negative), `history.length < threshold` is false and `window` can be empty, but `loopSignature(window[0])` is still called, which throws at runtime (`Cannot read properties of undefined`). If this threshold is configurable, one bad value can crash health evaluation instead of returning a normal signal/null result.\n\nUseful? React with 👍 / 👎.","url":"https://github.com/Doogie201/promptops-gui/pull/19#discussion_r2887425817"}]}}]}}}}}--- STDERR ---
EXIT_CODE: 0
