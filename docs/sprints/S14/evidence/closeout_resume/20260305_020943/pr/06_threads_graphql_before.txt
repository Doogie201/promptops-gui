CMD: gh api graphql -f query='query($owner:String!, $repo:String!, $number:Int!){ repository(owner:$owner,name:$repo){ pullRequest(number:$number){ reviewThreads(first:100){ nodes { id isResolved comments(first:20){ nodes { author { login } url body } } } } } } }' -F owner=Doogie201 -F repo=promptops-gui -F number=21
CWD: /Volumes/X9_DEV/Dev/Projects/promptops-gui
{"data":{"repository":{"pullRequest":{"reviewThreads":{"nodes":[{"id":"PRRT_kwDORZ8p_M5yP3Oc","isResolved":false,"comments":{"nodes":[{"author":{"login":"chatgpt-codex-connector"},"url":"https://github.com/Doogie201/promptops-gui/pull/21#discussion_r2888035564","body":"**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Clear active phase when processing phase_end**\n\n`computeDashboard` keeps `activePhase` unchanged on `phase_end`, so a stream like `phase_start(preflight)` then `phase_end(preflight)` still reports `activePhase: \"preflight\"` even though no phase is active. This makes the dashboard state internally inconsistent (`status: complete` but still shown as active) and can mislead operators in paused/partial runs where no next phase has started yet.\n\nUseful? React with 👍 / 👎."}]}},{"id":"PRRT_kwDORZ8p_M5yP3Oj","isResolved":false,"comments":{"nodes":[{"author":{"login":"chatgpt-codex-connector"},"url":"https://github.com/Doogie201/promptops-gui/pull/21#discussion_r2888035573","body":"**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Derive active agent before first switch event**\n\n`activeAgent` is only updated in `agent_switch_*` handlers, so runs with a single agent and no switch events will incorrectly show `activeAgent: null` even when events include an agent identifier (for example on `phase_start` in `agent_invocation`). This breaks the dashboard’s active-agent reporting for common no-switch executions.\n\nUseful? React with 👍 / 👎."}]}}]}}}}}--- STDERR ---
EXIT_CODE: 0
