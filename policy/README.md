# Policy

**Boundary**: Global constraints, compile-time constants, and typed limits (whitelists, budgets).
**Prohibited Imports**: Cannot import from ANY other local module. Must be a leaf node to avoid circular dependencies.
