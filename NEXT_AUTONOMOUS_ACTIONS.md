# Next Autonomous Actions

1. Add size-aware evidence guardrails for `docs/sprints/**/evidence` (hard-stop on runaway file growth signatures).
2. Add end-to-end template-to-evaluator scenario tests that include ticket factory outputs.
3. Add a deterministic report generator for sprint receipts into a single canonical JSON bundle.
4. Add branch/worktree compliance checks to `scripts/verify.sh` for operator preflight.
5. Add policy tests for whitelist coverage against actual touched paths in CI.
